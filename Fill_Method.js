// Fill Method  ; Fill can change your original array
// value , start, end

const myArr = new Array(10).fill(-1);
console.log(myArr);

const myA = [1,2,3,4,5,6,7,8];
myA.fill(0,2,5);  // change the value from  2 - 5 and put 0 instead of them; 
console.log(myA);

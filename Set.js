// Sets (It is Itearble)
// Store data in linearly form like array
// Set also have its own method
// No index-based access
// Order is not guarateed
// Unique items only (no duplicates allowed);

// Syntax;
const number = new Set([1,2,3]); // Set ma ak element ak hi bar store hoga duplicate not allowed
console.log(number);
console.log(number[1]); // -->> undefined
